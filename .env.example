# ============================================================================
# SUPABASE CONFIGURATION
# ============================================================================
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ============================================================================
# GOOGLE OAUTH CONFIGURATION
# ============================================================================
# Get these values from Google Cloud Console (APIs & Services > Credentials)
# See: https://console.cloud.google.com/

# Google OAuth 2.0 Client ID
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Google OAuth 2.0 Client Secret (KEEP THIS PRIVATE - Never commit to git)
# Store this securely in Supabase, not in your .env for production
VITE_GOOGLE_CLIENT_SECRET=your-google-client-secret

# ============================================================================
# OAUTH REDIRECT CONFIGURATION
# ============================================================================
# Use this to override the default redirect domain (useful for ngrok, tunnels, etc.)
# If not set, will use window.location.origin (localhost or current domain)
# Examples:
#   VITE_OAUTH_REDIRECT_DOMAIN=https://abc123.ngrok.io
#   VITE_OAUTH_REDIRECT_DOMAIN=https://zg5791kj-8080.inc1.devtunnels.ms
#   VITE_OAUTH_REDIRECT_DOMAIN=https://yourdomain.com
VITE_OAUTH_REDIRECT_DOMAIN=

# ============================================================================
# SUPABASE CONFIGURATION (from Supabase Dashboard)
# ============================================================================
# These are typically NOT needed in .env, but shown for reference
# Get from: Supabase Dashboard > Project Settings > General

# Your Supabase Project URL
# SUPABASE_URL=https://your-project.supabase.co

# Your Supabase Service Role Key (SECRET - use for server-side only)
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ============================================================================
# OPTIONAL: ENVIRONMENT FLAGS
# ============================================================================
# Set to 'production' when deploying
VITE_APP_ENV=development

# Enable debug logging for OAuth
VITE_DEBUG_OAUTH=true
